# Audit Log Schema

_To be written in Phase 3 alongside `AuditLog.ts` and `audit.service.ts` —
documents every field (timestamp, actor, action, input, decision, reasoning,
result) and the action taxonomy (query / verify / gate / payment_created /
payment_failed)._
